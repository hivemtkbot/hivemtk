# ============================================================
# user-web（商户端）环境变量示例
# 复制为 .env.development（本地开发）或 .env.production（生产构建）
# =============================================================
#
# 单一源约束（端口/URL 默认值）
# 单一代码源：hivemtk/user-server/internal/pkg/utils/config/ports.go
# 单一文档源：hivemtk/user-server/docs/dev/DEVELOPMENT.md §2.4 端口对照表
# 跨包对齐：
#   - user-server DefaultListenPort="8204"（vite.config.js proxy target 默认值）
#   - user-web dev port=8211（vite.config.js server.port）
# 严禁"软启动"——本文件端口字面量必须与 ports.go / DEVELOPMENT.md §2.4 严格一致。
# 调整流程：先改 ports.go → 同步本文件 → 同步 DEVELOPMENT.md §2.4
# ============================================================

# 运行环境
NODE_ENV=development

# API 服务地址（可配置，关键项）
# 留空或 / 表示同源相对路径，由 vite(本地 dev server) 代理 /api 转发到后端 user-server（本地 8204）
# 单一源：user-server/internal/pkg/utils/config/ports.go DefaultListenPort="8204"
# 文档源：user-server/docs/dev/DEVELOPMENT.md §2.4 端口对照表 | 8204 | user-server | Gin HTTP
# 跨域 / 远程独立部署时改为绝对地址，例如：
#   VITE_API_BASE_URL=https://user-api.your-domain.com
VITE_API_BASE_URL=/

# 是否开启 mock 数据（本地联调后端未就绪时可用）
VITE_USE_MOCK=false
