# 待改进任务清单（v1.0）

> **生成日期**: 2026-07-23
> **生成方法**: 深入理解 `docs/企业级架构优化/` 下 13 份文档 + `docs/architecture/GO_FIVE_LAYER_ARCHITECTURE.md` + `scripts/check-architecture.sh`，与 `user-server` / `user-web` 实际代码交叉对比验证，头脑清醒论证后整理。
> **交叉验证原则**:
> - 以**源码实地核验**为最高准绳（Grep/Glob/Read）
> - 当子文档与 `功能模块全景对齐清单.md`（v1.2，源码交叉验证版）冲突时，以对齐清单为准
> - 区分"文档有代码无"、"代码有文档无"、"两者不符"三种状态
> - 不臆测，每条任务均附代码证据

---

## 〇、文档清单与交叉验证范围

| 序号 | 文档 | 类别 | 交叉验证结论 |
|---|---|---|---|
| 1 | 五层架构治理任务清单.md | 架构治理 | 已有 T01-T09，本清单继承并更新状态 |
| 2 | 功能模块全景图.md | 模块全景 | 设计草案，以对齐清单 v1.2 纠偏为准 |
| 3 | 功能模块全景对齐清单.md | 模块全景 | **源码交叉验证版**，权威基线 |
| 4 | 六层物理拓扑清单.md | 物理拓扑 | 设计草案 |
| 5 | 六层物理拓扑对齐清单.md | 物理拓扑 | **源码交叉验证版**，权威基线 |
| 6 | 核心数据流向.md | 数据流 | 以对齐清单 + 实地核验为准 |
| 7 | 工具链调用逻辑.md | 工具链 | 部分工具名待核实 |
| 8 | 转人工门禁数据流向图.md | 门禁流程 | 代码已落地（HumanEscalationManager） |
| 9 | 认知决策大脑层.md | 决策层 | InferenceCycle 已落地，情境记忆待核实 |
| 10 | 坐席实时聊天看板.md | 功能特性 | 后端 API 已落地，前端三栏待核实 |
| 11 | 情绪感知与六维拟人度门禁判定.md | 算法门禁 | alignment_stage.go 已落地 |
| 12 | 渠道接入消息中台.md | 渠道中台 | Ingress 已落地，渠道未全收敛 |
| 13 | 资产包模式.md | 资产包 | 结构已落地，热插拔未实现 |

---

## 一、矛盾消解记录（头脑清醒论证）

> 本节记录交叉验证中发现的"子文档声明"与"源码事实"冲突，给出最终裁定，避免后续执行踩坑。

### 矛盾 1：6 维拟人度门禁 / 危机熔断是否已实现？

| 来源 | 声明 | 裁定 |
|---|---|---|
| 子代理初判 | "危机感打分与门禁决策自动化评估缺失（T02-06）" | ❌ 错误 |
| 功能模块对齐清单 v1.2 | `alignment_stage.go`（6 维）+ `gatekeeper_stage.go` + `HumanEscalationManager.TriggerCensorshipEscalation` ✅ 已落地 | ✅ 可信 |
| 源码实地核验 | `grep TriggerCensorshipEscalation` 命中 `internal/service/human_escalation.go` | ✅ 确认已实现 |

**裁定**: 6 维门禁与危机熔断**已实现**，不列入待改进任务。

### 矛盾 2：L1/L2 记忆读取是否未实现？

| 来源 | 声明 | 裁定 |
|---|---|---|
| 子代理初判 | "L1 短期上下文与 L2 长期画像读取未实现（T02-01）" | ❌ 不准确 |
| 功能模块对齐清单 v1.2 | `MemorySystem` 四层 API（L1Append/L2SaveFact/Remember/Recall）✅ 已落地 | ✅ 可信 |
| 六层物理拓扑对齐清单 v1.1 | L1-L4 以 `MemorySystem` 统一入口 ✅ | ✅ 可信 |

**裁定**: MemorySystem API **已实现**，不列入待改进任务。

### 矛盾 3：渠道适配器是否全量走 Ingress？

| 来源 | 声明 | 裁定 |
|---|---|---|
| 功能模块对齐清单 v1.2 验证清单 | `[ ] 各渠道适配器全部走 InboxIngressService.HandleIngressMessage（当前部分渠道仍为独立路径，待收敛）` | ⚠️ 真实缺口 |
| 源码实地核验 | `grep HandleIngressMessage internal/channelbot/` 0 命中 | ✅ 确认未收敛 |

**裁定**: 渠道收敛**真实缺口**，列入任务 T10。

### 矛盾 4：资产包热插拔是否实现？

| 来源 | 声明 | 裁定 |
|---|---|---|
| 资产包模式.md | 描述资产包加载/配置/热插拔机制 | 文档声明 |
| 源码实地核验 | `grep HotPlug\|hot_plug\|热插拔 internal/` 0 命中 | ❌ 未实现 |

**裁定**: 资产包热插拔**未实现**，列入任务 T14。

---

## 二、待改进任务清单

> 编号规则：A=架构治理（继承自五层架构治理任务清单）、B=渠道/消息中台、C=坐席看板前端、D=资产包、E=认知决策层、F=工具链。
> 优先级：P0=必须 / P1=强烈建议 / P2=建议 / P3=可延后。

---

### A. 五层架构治理（继承自《五层架构治理任务清单.md》，状态更新至 2026-07-23）

> 完整明细见原清单，此处仅列**仍未完成**项。

#### A1 · Model 业务方法清理（原 T04）— P1 🚫

| 项 | 内容 |
|---|---|
| 来源 | 五层架构治理任务清单.md §三-A |
| 当前状态 | 多个 model 文件含业务方法（非 GORM Hook），违反 §3.6 |
| 违规文件 | `internal/model/customer.go`、`auto_reply_account.go`、`obs_config.go`、`douyin_card.go`、`tiktok_card.go`、`xianyu_card.go`、`xiaohongshu_card.go`、`live_code.go`、`live_code_qr.go`、`chat_channel.go`、`customer_tag.go` |
| 改进动作 | 把 Model 业务方法下沉到对应 `<domain>_service.go`；Model 仅保留 `TableName()` + Hook |
| 验证 | `bash scripts/check-architecture.sh` §4 通过 0 违规 |

#### A2 · Service 反向依赖 db.GetDB() 残留（原 T05）— P0 🚧

| 项 | 内容 |
|---|---|
| 来源 | 五层架构治理任务清单.md §三-A |
| 当前状态 | 仅剩 2 处真违规（`unified_message.go:244-247, 291-294`） |
| 违规点 | `NewAutoReplyRuleRepository()` / `NewKnowledgeRepository()` 在 service 包内定义并调 `_db.GetDB()` |
| 改进动作 | 把两个 Repository 整体迁到 `internal/repository/`；Service 改依赖注入；新 repo 方法如需 ctx 用 `context.TODO()` + `// TODO(ctx-专项)` 占位 |
| 验证 | `grep "db.GetDB()" internal/service/` 0 命中（非测试非注释） |

#### A3 · 工具层 Port 模式未完整移除 CustomerService（原 T07）— P1 🚫

| 项 | 内容 |
|---|---|
| 来源 | 五层架构治理任务清单.md §三-A |
| 当前状态 | `customer_tools.go:51` 仍持有 `*service.CustomerService` 字段作回退路径 |
| 违规点 | `CustomerService` 字段 + `NewCustomerTools` 默认赋值 + 4 处 `else if t.deps.CustomerService != nil` 回退分支 |
| 改进动作 | 删除字段、删除默认赋值、删除 4 处回退分支，统一走 `portcontract.CustomerPort`；装配层 `tool_executor_wiring.go` 强制注入 Port Adapter |
| 验证 | `grep -rn "CustomerService" internal/aiagent/agent/tooluse/` 仅剩注释与 Port Adapter 引用 |
| 硬约束 | project_memory："工具层需完整走 Port 模式，移除对 CustomerService 字段的依赖" |

#### A4 · 平台端 check-architecture.sh 缺失（原 T08）— P2 🚫

| 项 | 内容 |
|---|---|
| 来源 | 五层架构治理任务清单.md §三-A |
| 当前状态 | `hivemtk-platform/platform-server/scripts/` 仅有 `generate-swagger.sh`，无检查脚本 |
| 改进动作 | 复制 `hivemtk/scripts/check-architecture.sh` 并适配平台端路径常量；运行生成违规清单；修复 `admin_service.go:24` 的 `adminDB()` 包装 |
| 验证 | 平台端检查脚本可执行并输出报告 |

#### A5 · 端到端架构检查全绿（原 T09）— P0 🚫（阻塞于 A4）

| 项 | 内容 |
|---|---|
| 来源 | 五层架构治理任务清单.md §三-A |
| 前置依赖 | A4 必须先完成 |
| 完成标准 | 两端 `check-architecture.sh` 0 error 0 warn（§9 ctx 透传 skip 除外） |

#### A6 · .bak 僵尸文件清理（清扫项）— P3 🧹

| 项 | 内容 |
|---|---|
| 来源 | 五层架构治理任务清单.md §三-附 |
| 当前状态 | `internal/service/*.bak` 48+ 个、`internal/service/confidence/*.bak` 2+ 个 |
| 改进动作 | `find hivemtk/user-server -name "*.bak" -type f -delete`（提交前 `git status` 确认未跟踪） |
| 硬约束 | project_memory："删除 orphan 文件、zombie 文件" |

---

### B. 渠道接入消息中台收敛（源自功能模块全景对齐清单 v1.2 未勾选项 + 渠道接入消息中台.md）

#### B1 · 各渠道适配器全量走 InboxIngressService — P1 🚫

| 项 | 内容 |
|---|---|
| 来源 | 功能模块全景对齐清单.md 验证清单（唯一未勾选项）+ 渠道接入消息中台.md |
| 当前状态 | `InboxIngressService.HandleIngressMessage` 已落地（`internal/service/inbox_ingress.go`），但 `internal/channelbot/*`（telegram/whatsapp）当前无直接引用，仍走独立路径 |
| 代码证据 | `grep HandleIngressMessage internal/channelbot/` → 0 命中 |
| 改进动作 | 把 `channelbot/telegram/`、`channelbot/whatsapp/` 等渠道入站消息统一改为调 `InboxIngressService.HandleIngressMessage`，实现标准化 `MessageEvent` + 人工锁 + AI 串行锁 + pending 队列 |
| 验证 | 各渠道 webhook 处理函数均经 Ingress 标准化；`grep HandleIngressMessage internal/channelbot/` 有命中 |
| 影响范围 | `internal/channelbot/telegram/*.go`、`internal/channelbot/whatsapp/*.go`、闲鱼/平台机器人浏览器接入 |

---

### C. 坐席实时聊天看板前端完善（源自坐席实时聊天看板.md）

#### C1 · Vue 3 三栏工作台前端核验与补全 — P1 🚫

| 项 | 内容 |
|---|---|
| 来源 | 坐席实时聊天看板.md §一 |
| 当前状态 | 仅 `user-web/src/views/customerSession/List.vue` 单文件；含左栏会话列表 + 状态切换，中栏聊天窗与右栏客户 360° 画像需核验完整性 |
| 文档要求 | 三栏物理布局：左栏会话队列 / 中栏聊天窗 / 右栏客户 360° 画像 + SOP 控制 |
| 改进动作 | 核验 `List.vue` 是否已含三栏；若中栏/右栏缺失或为占位，补全为完整三栏工作台组件 |
| 验证 | 三栏布局渲染正常，WebSocket 实时推送生效 |
| 硬约束 | project_memory："坐席实时聊天看板采用 Vue 3 三栏工作台布局"；UI 仅允许改样式与小布局 |

#### C2 · 黑名单前端交互补全 — P1 🚫

| 项 | 内容 |
|---|---|
| 来源 | 坐席实时聊天看板.md §二 + project_memory |
| 当前状态 | 后端 4 个路由（blacklist / remove / check / list）已在 `service_routes.go` 注册；`UserBlacklist` 模型采用 user_id 维度 + TTL + 软删除 |
| 改进动作 | 核验前端是否有拉黑/解黑/黑名单列表 UI 交互；缺失则补全 |
| 验证 | 拉黑后该访客消息被拦截；解黑恢复；黑名单列表分页正常 |
| 硬约束 | project_memory："坐席实时聊天看板需实现拉黑功能，UserBlacklist 模型采用 user_id 维度、TTL 过期和软删除机制" |

#### C3 · AI/人工切换前端交互补全 — P1 🚫

| 项 | 内容 |
|---|---|
| 来源 | 坐席实时聊天看板.md §三 |
| 当前状态 | 后端 `takeover` / `release` / `switch-handler` 路由已注册 |
| 改进动作 | 核验前端中栏顶栏是否有"AI 托管 / 人工接管"切换 UI；缺失则补全 |
| 验证 | 切换后 handler_type 正确变更，WebSocket 推送访客 + 坐席端 |
| 硬约束 | project_memory："坐席实时聊天看板需实现 AI/人工切换功能" |

---

### D. 资产包模式热插拔（源自资产包模式.md）

#### D1 · 资产包热插拔 / 动态配置机制实现 — P2 🚫

| 项 | 内容 |
|---|---|
| 来源 | 资产包模式.md |
| 当前状态 | 资产包五层架构代码已落地（controller/service/repository/model/dto + migration + routes）；`Weave(in WeaveInput)` 织布函数已实现；**但热插拔/动态加载机制未实现** |
| 代码证据 | `grep HotPlug\|hot_plug\|热插拔 internal/` → 0 命中 |
| 改进动作 | 根据资产包模式.md 设计，实现资产包的动态启用/禁用、配置热更新、版本切换 |
| 验证 | 资产包启用/禁用后 Weave 织布行为即时变更，无需重启服务 |

---

### E. 认知决策大脑层情境记忆（源自认知决策大脑层.md）

#### E1 · 情境记忆系统完整性核验 — P2 ⚠️

| 项 | 内容 |
|---|---|
| 来源 | 认知决策大脑层.md |
| 当前状态 | `InferenceCycle` 主编排已落地（感知→对齐→门禁→规划）；`MemorySystem` L1-L4 已落地；但文档描述的"情境记忆"（任务执行历史、跨会话上下文记忆）需核验是否完整 |
| 改进动作 | 对照认知决策大脑层.md，核验 `dataflow_orchestrator.go` 与 `MemorySystem` 是否覆盖文档要求的情境记忆能力；缺口补全 |
| 验证 | 跨会话情境记忆可被 InferenceCycle 读取并影响决策 |

---

### F. 工具链调用逻辑完整性（源自工具链调用逻辑.md）

#### F1 · 工具链调用完整性核验 — P2 ⚠️

| 项 | 内容 |
|---|---|
| 来源 | 工具链调用逻辑.md |
| 当前状态 | `ToolRouter.Route` + 6 大工具域（customer/reach/pm/knowledge/business）已落地；装饰器链（限流/重试/熔断/审计/计费）已落地 |
| 改进动作 | 对照工具链调用逻辑.md 列出的工具清单，逐个核验每个工具名是否有对应实现（如 `reach.send_whatsapp`、`business.create_order` 等）；缺失补全或从文档移除 |
| 验证 | 文档列出的工具名与 `tooluse/*_tools.go` 注册名一一对应 |

---

## 三、任务总览与优先级矩阵

| 编号 | 任务 | 优先级 | 状态 | 来源文档 | 预估影响 |
|---|---|---|---|---|---|
| A1 | Model 业务方法清理 | P1 | 🚫 | 五层架构治理 | 11 个 model 文件 |
| A2 | Service db.GetDB() 残留 | P0 | 🚧 2处 | 五层架构治理 | 2 处 + 新建 2 个 repo |
| A3 | 工具层 Port 完整移除 | P1 | 🚫 | 五层架构治理 | customer_tools.go + wiring |
| A4 | 平台端检查脚本 | P2 | 🚫 | 五层架构治理 | 新建脚本 + admin_service 修复 |
| A5 | 端到端检查全绿 | P0 | 🚫 | 五层架构治理 | 阻塞于 A4 |
| A6 | .bak 僵尸文件清理 | P3 | 🚫 | 五层架构治理 | 删除 50+ 文件 |
| B1 | 渠道适配器全量走 Ingress | P1 | 🚫 | 渠道接入消息中台 | channelbot/* 改造 |
| C1 | Vue 3 三栏工作台补全 | P1 | 🚫 | 坐席实时聊天看板 | user-web 前端 |
| C2 | 黑名单前端交互补全 | P1 | 🚫 | 坐席实时聊天看板 | user-web 前端 |
| C3 | AI/人工切换前端补全 | P1 | 🚫 | 坐席实时聊天看板 | user-web 前端 |
| D1 | 资产包热插拔实现 | P2 | 🚫 | 资产包模式 | asset_bundle 服务层 |
| E1 | 情境记忆完整性核验 | P2 | ⚠️ | 认知决策大脑层 | dataflow_orchestrator |
| F1 | 工具链完整性核验 | P2 | ⚠️ | 工具链调用逻辑 | tooluse/*_tools.go |

---

## 四、执行约束

1. **避让范围**（与五层架构治理任务清单一致）：
   - Repository 716 个方法 ctx 透传（独立专项）
   - 多 AI 智能体整体重构（独立专项）
   - 数据库 schema 变更（独立专项）
   - `success(c,nil)` 占位全面下沉（独立专项）

2. **执行顺序建议**：
   - P0 优先：A2（2 处真违规）→ A4（脚本就位）→ A5（全绿收尾）
   - P1 批次：A1 / A3 / B1 / C1-C3 可并行（不同模块，无冲突）
   - P2 批次：D1 / E1 / F1 在 P1 完成后推进
   - P3：A6 清扫随时可做

3. **每完成一个任务立即 git commit**（遵循 project_memory Git 纪律）

4. **每个任务前后**运行 `bash scripts/check-architecture.sh` 记录基线与验证

5. **冲突回避**：任一任务若与其他在跑专项冲突，立即 `blocked` 标记 + 跳到下一项

6. **不修改 ctx 透传**：严禁在 `repository/*.go` 中"顺便把 ctx 也加进去"

---

## 五、已验证落地（不列入待改进，备查）

> 以下文档声明的能力经源码交叉验证确认**已实现**，避免重复劳动。

| 能力 | 代码路径 | 验证方式 |
|---|---|---|
| 6 维拟人度门禁 | `runtime/alignment_stage.go` | 功能模块对齐清单 ✅ |
| 危机熔断 + 转人工 | `runtime/gatekeeper_stage.go` + `service/human_escalation.go` | grep TriggerCensorshipEscalation 命中 |
| InferenceCycle 主编排 | `runtime/inference_cycle.go` | 功能模块对齐清单 ✅ |
| MemorySystem L1-L4 | `service/memory_system.go` | 功能模块对齐清单 ✅ |
| ToolRegistry + ToolRouter | `tooluse/tool_router.go` | 功能模块对齐清单 ✅ |
| Ingress 标准化入口 | `service/inbox_ingress.go` | 功能模块对齐清单 ✅ |
| 本地推理栈（llama.cpp + TEI） | `docker-compose.yml` + `aiagent/llm/dispatcher.go` | 功能模块对齐清单 ✅ |
| 资产包 Weave 织布 | `service/asset_bundle.go` Weave() | 功能模块对齐清单 ✅ |
| customer_session 拆分 | `service/customer_session_*.go`（7 子文件） | 五层架构治理清单 ✅ |
| Controller c.JSON 违规 | 已修复 | 五层架构治理清单 ✅ |
| Controller 直引 repository | 已修复 | 五层架构治理清单 ✅ |
| pkg/utils 通用文件名清理 | 已按业务域拆分 | 五层架构治理清单 ✅ |

---

## 六、相关文档索引

- 五层架构规范：`docs/architecture/GO_FIVE_LAYER_ARCHITECTURE.md`
- 架构检查脚本：`scripts/check-architecture.sh`
- 五层架构治理任务清单：`docs/企业级架构优化/五层架构治理任务清单.md`
- 功能模块全景对齐清单：`docs/企业级架构优化/功能模块全景对齐清单.md`
- 六层物理拓扑对齐清单：`docs/企业级架构优化/六层物理拓扑对齐清单.md`
- 审计复审报告：`docs/audit/审计文档复审报告.md`

---

*本清单基于 2026-07-23 代码实地交叉验证生成。执行时从 P0 任务开始，逐项推进。*

---

## 七、执行结果记录（2026-07-23 一次性执行）

> 13 项任务由 5 个并行子代理 + 1 个收尾子代理执行，全部交付。

### 执行总览

| 编号 | 任务 | 优先级 | 状态 | Commit | 说明 |
|---|---|---|---|---|---|
| A1 | Model 业务方法清理 | P1 | ✅ | `bf5c28e` | 11 个 model 文件业务方法下沉 service；5 个清单外文件留待 A5 |
| A2 | Service db.GetDB() 残留 | P0 | ✅ | `ba164f5` | AutoReplyRule/Knowledge Repository 迁移到 repository 层 |
| A3 | 工具层 Port 完整移除 | P1 | ✅ | `4b1233f` | CustomerService 字段及 4 处回退分支已删除 |
| A4 | 平台端检查脚本 | P2 | ✅ | `fcb6d99` | 新建平台端 check-architecture.sh + admin_service 下沉 repository |
| A5 | 端到端检查全绿 | P0 | 🚧 部分 | `310c8b2` | §3 已清理；§1/§4/§6 阻塞于 ctx 透传专项并行，待专项完成后收尾 |
| A6 | .bak 僵尸文件清理 | P3 | ✅ | 无 | 删除 317 个未跟踪 .bak 文件（git 不记录） |
| B1 | 渠道适配器走 Ingress | P1 | ✅ | `9e9d6a4` | telegram/whatsapp 经依赖反转走 HandleIngressMessage |
| C1 | Vue 3 三栏工作台 | P1 | ✅ | `a427318` | 三栏布局已完整，补全状态指示灯样式 |
| C2 | 黑名单前端交互 | P1 | ✅ | `bba86ed` | 拉黑/解黑/列表 UI 接入真实 API |
| C3 | AI/人工切换前端 | P1 | ✅ | `cb93f2c` | 统一走 switch-handler 接口 |
| D1 | 资产包热插拔 | P2 | ✅ | `43c3d59` | EnableBundle/DisableBundle/GetEnabledBundles + REST 接口 |
| E1 | 情境记忆核验 | P2 | ✅ | `b6cf1b6` | 发现真实缺口并补全：InferenceContext 增 EpisodicMemory 字段 |
| F1 | 工具链核验 | P2 | ✅ | 无 | 文档措辞差异，非功能缺口，无需改动 |

### A5 阻塞说明

A5 端到端检查未完全全绿，原因：
- **§9 ctx 透传（14→570 错误）**：属独立专项，明确 skip
- **§1/§4/§6 残余违规**：ctx 透传专项（T05）正在并行修改 controller/service/repository/model 文件，引入大量临时违规，文件状态不稳定，无法安全清理
- **§3 已全绿**（commit `310c8b2`）：repository 反向依赖 service/dto 已修复

**收尾条件**：ctx 透传专项完成后，文件状态稳定时，再清理 §1（controller 直调 db/gorm）、§4（5 个清单外 model 文件）、§6（4 个文件命名 + helpers.go）。

### 架构检查违规数变化

| 端 | 执行前 | A1-A4/D1/B1/C1-C3/E1 后 | §3 修复后 | 备注 |
|---|---|---|---|---|
| user-server | 39 | 28 | 14（仅 §9 ctx） | 非 ctx 透传违规已清理至 0（§3 后），后因 T05 并行干扰回升 |
| platform-server | — | 78 | 78 | 全为 §9 ctx 透传，A4 脚本就位 |

### 关键设计决策

1. **B1 依赖反转**：channelbot 不能反向 import service（import cycle），在 `channelbot/core` 定义 `IngressHandler` 接口，service 用 adapter 实现，解决跨层依赖
2. **D1 冷启动兼容**：热插拔缓存空时走 permissive 回退（放行全部），启用后严格管控，保证现有测试不破坏
3. **E1 真实缺口**：InferenceCycle 此前完全不引用 MemorySystem，情境记忆无法影响决策；补全 `EpisodicMemoryProvider` 接口 + `InferenceContext.EpisodicMemory` 字段 + planner 注入
4. **A6 未 commit**：317 个 .bak 文件均未被 git 跟踪，删除不产生 git 变更，避免误提交其他子代理 WIP
