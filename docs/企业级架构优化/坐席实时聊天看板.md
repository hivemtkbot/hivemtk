这是商户老板和客服人员每天驻留的界面，其功能架构分为三个物理分栏

+--------------------------------------------------------------------------------------------------+

| hivemtk 王牌坐席工作台 (Vue 3 现代化三栏布局)                                                      |
+--------------------------------------------------------------------------------------------------+

| 【左栏：会话排队列表】         | 【中栏：标准全媒体聊天窗】         | 【右栏：客户 360° 画像与 SOP 控制】   |
|                               |                                  |                                  |
|  [🔍 搜索手机号/渠道ID]        |  [🤖 AI自动托管中] (顶栏绿灯)     |  [🧑‍💻 当前状态]                    |
|                               |  ------------------------------  |  ( ) AI 托管  (●) 人工接管 (切换) |
|  ● WA: +49 17612345 (托管)    |  客户 (16:01):                   |                                  |
|    "蓝莓味还有货吗？"          |  "我买的快递到哪了？单号HIVE123" |  [L2 长期画像标签 (PGvector)]     |
|                               |                                  |  - 用户地区：德国 (德国仓发货)    |
|  🔴 TG: Crypto_King (报警)     |  AI数字员工 (16:01):             |  - 偏好口味：蓝莓薄荷 / 冰爽西瓜   |
|    "骗子！我要退款！" ──> [叮咚] |  *思考：用户查询物流，触发工具*  |  - 历史标签：高频复购潜客         |
|                               |  "已经为您查到啦！您的顺丰包裹...|                                  |
|  ● Web: 匿名访客 #08 (人工)    |                                  |  [L3 当前 SOP 业务阶段]           |
|    坐席张三接管中...           |  ==============================  |  ( ) 售前询价  ( ) 引导留资       |
|                               |  [ 输入框：人工在此打字直接覆盖]  |  (●) 售后查单  ( ) 投诉退款 (强更) |
|                               |  [ 🎟️ 发优惠券 ] [ 🛒 发商品卡片 ]|                                  |
+----------------------------------------------------------------------------

## 一、设计目标

- **三栏物理布局**：左栏会话队列 / 中栏聊天窗 / 右栏客户 360° 画像
- **AI/人工一键切换**：前端通过统一 `switchSessionHandler(id, handlerType)` 切换
- **零身份伪造**：所有 sender_type / agent_id 由后端从 JWT 上下文派生，前端不可伪造
- **实时推送**：WebSocket 推送新消息、会话状态变更、AI 建议给坐席端
- **客户 360°**：跨会话聚合画像标签、SOP 阶段、历史消息统计

## 二、核心数据模型

| 模型 | 用途 |
| --- | --- |
| `CustomerSession` | 单次会话主表（含 `handler_type` 字段标识 AI/人工） |
| `SessionMessage` | 会话消息流（含 `sender_type`: user/ai/agent） |
| `AgentStatus` | 坐席状态（online/busy/offline + max_sessions） |
| `UserBlacklist` | 用户黑名单（user_id 维度，支持 TTL 过期） |
| `AISuggestion` | AI 实时建议（按会话 ID 聚合） |
| `QuickReply` | 快捷回复（按 category 分类） |
| `SessionTag` | 会话标签（支持搜索/添加/移除） |

## 三、AI/人工接管与切换

### 3.1 路由清单（service_routes.go）

| 路径 | 方法 | 说明 |
| --- | --- | --- |
| `/api/customer-sessions/:id/takeover` | POST | 坐席接管 AI 会话 |
| `/api/customer-sessions/:id/release` | POST | 坐席释放回 AI |
| `/api/customer-sessions/:id/switch-handler` | POST | 统一切换接口（前端推荐） |
| `/api/customer-sessions/:id/transfer` | POST | 转接给其他坐席 |
| `/api/customer-sessions/:id/blacklist` | POST | 拉黑该会话对应的访客 |
| `/api/customer-sessions/blacklist/remove` | POST | 解除拉黑 |
| `/api/customer-sessions/blacklist/check` | GET | 查询访客是否在黑名单 |
| `/api/customer-sessions/blacklist` | GET | 黑名单分页列表 |

### 3.2 接管 / 释放 / 切换（service/customer_session.go）

- **TakeoverByAgent(ctx, req)**：
  1. 校验坐席存在 + 在线 + 容量未满
  2. 幂等：会话已分配给该坐席 → 仅切 handler/status
  3. 接管：减原坐席活跃数 + 分配新坐席 + 加新坐席活跃数
  4. 写 Redis 人工锁（`LockSessionForHuman`，让 AI 推理不再继续）
  5. 推 WebSocket 给访客（`type=agent_joined, handler=human`）和坐席（`session_update`）

- **ReleaseToAI(ctx, req)**：
  1. 校验会话归属该坐席（防越权）
  2. 清空 agent_id / agent_name
  3. handler_type 切回 ai、status 切回 waiting
  4. 减坐席活跃数 + 解 Redis 人工锁
  5. 推 WebSocket 通知

- **SwitchHandler(ctx, req)**：前端统一入口
  - `handler_type=human` → 等价 Takeover
  - `handler_type=ai` → 等价 Release

### 3.3 关键安全设计

- `agent_id` 从 `ctx.user_id`（JWT 中间件注入）派生，前端**禁止伪造**
- 访客请求（带 `chat_channel_id`）的 `sender_type` 被强制覆盖为 `user`
- 所有 WebSocket 推送仅按 agent_id 路由，访客按 session_id 路由

## 四、拉黑 / 解除拉黑

### 4.1 模型 `UserBlacklist`（model/user_blacklist.go）

```go
type UserBlacklist struct {
    ID, OperatorID  uint
    UserID          string         // 维度
    Platform        Platform
    Reason, Source  string         // manual / auto / risk
    SessionID       string         // 拉黑时所在会话（便于审计）
    Active          bool           // 软删除标志
    ExpiresAt       *time.Time     // nil = 永久
    CreatedAt, UpdatedAt time.Time
}
```

- 表名 `user_blacklist`
- 索引：`(user_id, platform, active)`、`session_id`、`operator_id`

### 4.2 拉黑流程（service BlacklistUser）

1. 校验会话存在且 `user_id` 非空
2. 幂等：若 `user_id+platform` 已有 active 记录 → 更新 reason / expires_at
3. 写黑名单（`Active=true`，TTL 可选）
4. **关闭该会话**（`status=closed`）防止继续对话
5. 推 WebSocket：前端 `event=blacklisted`、访客 `handler=human + blacklisted=true`
6. 后续该 user_id 在该 platform 上所有新会话都应被 `IsUserBlacklisted()` 拒绝

### 4.3 解除拉黑

`UnblacklistUser(userID, platform)`：软删除（`Active=false`），保留审计轨迹。

### 4.4 前端交互

- 右栏"快捷操作" → 点击「拉黑」按钮
- 弹出 prompt 输入拉黑原因
- 调 `blacklistSession(id, reason, ttlHours=0)` → 后端关会话 + 写黑名单
- 列表自动刷新（`loadSessions()`）

## 五、客户 360° 画像

- **基本统计**：`getCustomerStats(userId)` → 消息数 / 历史会话数 / AI 回复数
- **长期画像标签**：`getCustomerTags(userId)` → PGvector + L2 长期记忆
- **SOP 阶段**：右栏 `el-radio-group` 切换 + 保存（`saveSopStage()`）
- **快捷操作**：发券 / 发商品卡 / 拉黑 / 结束

## 六、测试覆盖

| 测试文件 | 用例数 | 覆盖范围 |
| --- | --- | --- |
| `customer_session_test.go` | 15 | CRUD / 分页 / 分配 / 评分 / 转接 |
| `customer_session_takeover_test.go` | 11 | 接管 / 释放 / 切换 + 越权 / 离线 / 容量 |
| `customer_session_blacklist_test.go` | 9 | 拉黑 / 解除 / TTL / 幂等 / 分页 / 边界 / **CreateSession 串联拒绝 / 跨 platform 独立** |

## 七、待办 / BLOCKED

- **BLOCKED-import-cycle**：项目预存在 import cycle（`service ↔ tooluse`），导致 `go build ./...` 与 `go test ./internal/service` 无法运行。
  需独立架构任务：把 `tooluse` 对 `service` 的依赖通过 contract/port 包解耦（参考 `tool_ports_adapter.go` 的 Port 模式）。
  短期绕过：`go build ./internal/model ./internal/repository ./internal/controller`（不含 service 全图）单包验证。
